import { GlobalStyles } from '@styles/globalStyles';
import theme from '@styles/themes';

function App() {
  return (
    <div className="app">
      <GlobalStyles theme={theme} />
    </div>
  );
}

export default App;
